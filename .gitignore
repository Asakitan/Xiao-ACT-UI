# ── Python cache ──────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
!_sao_cy*.pyd
*.pyo

# ── User data (excluded from repo) ────────────────
player_profile.json
player_cache.json
settings.json

# ── Debug / log files ─────────────────────────────
packet_debug.jsonl
*.log
*.jsonl
exports/

# ── Test files ────────────────────────────────────
test_*.py
*_test.py
tests/

# ── Build / dist ──────────────────────────────────
dist/
build/
*.exe

# ── Packaging specs (tracked) ─────────────────────
# XiaoACTUI.spec and server/AutoKeyServer.spec are tracked
# build.ps1 is tracked

# ── IDE / OS ──────────────────────────────────────
.vscode/
.idea/
Thumbs.db
.DS_Store
server/data/
server/*.db
temp/

# ── Dev publish tool (local only, contains API key) ───
dev_publish.py
dev_publish.bat
dev_publish_gui.py
dev_publish_config.json
update_host/update_host_config.json

# ── Updater runtime artifacts ─────────────────────
update_state.json
staging/
backup/
update_apply.log
update_host/releases/

# ── Memory probe research (local only, IL2CPP dumps) ───
tools/mem_probe/

# ── Misc ───────────────────────────────────────────
.github/
