# ── Python cache ──────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
!_sao_cy*.pyd
*.pyo
plan_*

# ── User data (excluded from repo) ────────────────
player_profile.json
player_cache.json
settings.json

# ── Debug / log files ─────────────────────────────
packet_debug.jsonl
*.log
*.jsonl
exports/

# ── Local live name-table diagnostics (regenerated on demand) ───────
assets/name_tables/live_*.json
assets/name_tables/live_name_table_scan_report_*.md
assets/name_tables/_backup_live_scan_*/
assets/name_tables/tcp_preparse_name_cache.local.json
!assets/name_tables/tcp_preparse_name_cache.json
!assets/name_tables/dungeon.json
!assets/name_tables/monster.json
!assets/name_tables/boss.json
!assets/name_tables/boss_mechanic.json
!assets/name_tables/buff.json

# ── Build / dist ──────────────────────────────────
dist/
build/
*.exe

# ── .NET build artifacts ──────────────────────────
**/bin/
**/obj/

# ── Packaging specs (tracked) ─────────────────────
# XiaoACTUI.spec and server/AutoKeyServer.spec are tracked
# build.ps1 is tracked

# ── IDE / OS ──────────────────────────────────────
.vscode/handoff/
.vscode/
.idea/
Thumbs.db
.DS_Store
server/data/
server/*.db
temp/

# ── Dev publish tool (local only, contains API key) ───
dev_publish.py
dev_publish.bat
dev_publish_gui.py
dev_publish_config.json
update_host/update_host_config.json

# ── Updater runtime artifacts ─────────────────────
update_state.json
staging/
backup/
update_apply.log
update_host/releases/

# ── mem_probe production: keep code, exclude per-user anchor data ───
mem_probe/anchors.json

# ── Misc ───────────────────────────────────────────
.github/
out/
_cache/
bin/
.codex/
