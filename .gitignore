# ── Python cache ──────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
*.pyo
plan_*

# ── Cython source (only .pyd binaries shipped) ───
python/_sao_cy_*.pyx
python/mem_probe/_sao_cy_*.pyx
python/mem_probe/rt_io.py
python/mem_probe/rt_io_proxy.py
python/plugins/star_resonance_plugin/cython/_sao_cy_*.pyx

# ── User data (excluded from repo) ────────────────
player_profile.json
player_cache.json
settings.json
settings.json.corrupt*

# ── Debug / log files ─────────────────────────────
packet_debug.jsonl
*.log
*.jsonl
exports/

# ── Local live name-table diagnostics (regenerated on demand) ───────
python/assets/name_tables/live_*.json
python/assets/name_tables/live_name_table_scan_report_*.md
python/assets/name_tables/_backup_live_scan_*/
python/assets/name_tables/tcp_preparse_name_cache.local.json
python/assets/name_tables/boss_skill_observations.local.json
!python/assets/name_tables/tcp_preparse_name_cache.json
!python/assets/name_tables/dungeon.json
!python/assets/name_tables/monster.json
!python/assets/name_tables/boss.json
!python/assets/name_tables/boss_mechanic.json
!python/assets/name_tables/buff.json

# ── Build / dist ──────────────────────────────────
dist/
build/
*.exe

# ── Roslyn C# compiler (downloaded, rebuilt by fetch_roslyn) ──
python/act_platform/scripting/roslyn/

# ── .NET build artifacts ──────────────────────────
**/bin/
**/obj/

# ── Packaging specs (tracked) ─────────────────────
# XiaoACTUI.spec and server/AutoKeyServer.spec are tracked
# build.ps1 is tracked

# ── IDE / OS ──────────────────────────────────────
.vscode/handoff/
.vscode/
.idea/
Thumbs.db
.DS_Store
python/server/data/
python/server/*.db
temp/

# ── Dev publish tool (local only, contains API key) ───
dev_publish.py
dev_publish.bat
dev_publish_gui.py
dev_publish_config.json
python/update_host/update_host_config.json

# ── Updater runtime artifacts ─────────────────────
update_state.json
staging/
backup/
update_apply.log
python/update_host/releases/

# ── mem_probe production: keep code, exclude per-user anchor data ───
python/mem_probe/anchors.json

# —— Local only, not part of the repo ─────────────────────────────────
drivers/
locale/
driver_*.py
rt_io.py
_dc.py
_vf.py
post_build_harden.py
_sao_cy_wnd.pyx
_encrypt_drivers.py
_build_driver.py
_convert_docstrings.py

# —— License system (local only, never commit) ────────────────────────
python/license/
python/license_server/
license_tool.py
.sao_license.dat
*.license
license.db

# —— Plugin native protection (encryption/anti-tamper, local only, never commit) ──
python/act_platform/protect/
python/update_host/build_service.py
python/update_host/toolchain_bootstrap.py
python/update_host/native_crypto.py
*.pyd.sao
*.native_cache/

# —— Platform source vault (仓库静态加密保护，见 pyx_vault.py) ──────────────
# 明文源码只留本地磁盘，进 git 的是同目录下的 *.vault 加密件；密钥不进仓库。
python/.vault/
python/mem_probe/_sao_cy_memscan.pyx

# ── AI Editor (user conversation history, local only) ──
python/ai_editor_history/
python/.sao/chat_history/
.vscode/handoff/

# ── AI Editor installed extensions (machine-specific, can be 100s of MB) ──
python/ai_editor_extensions/

# ── AI Editor real-VSIX smoke test output (machine-specific, regenerated) ──
python/ai_editor/real_extension_smoke_results.json


# ── Embedded Node.js runtime (downloaded binary, ~70 MB) ──
python/runtime/node/node.exe

# ── Misc ───────────────────────────────────────────
.github/
out/
_cache/
bin/
.codex/
