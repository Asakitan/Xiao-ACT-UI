{
  "id": "live_dps_overlay_plugin",
  "name": "Live DPS Overlay",
  "version": "1.0.0",
  "entry": "plugin.py",
  "description": "Flagship UI-redraw example: a register_ui_panel live DPS board that repaints on every act_snapshot, plus an opt-in compact overlay pinned onto the DPS meter surface (Entity + WebView).",
  "enabled": true,
  "subscriptions": ["act_snapshot", "encounter_finalized"],
  "capabilities": [
    "plugin_manager",
    {"id": "ui_panels", "title": "Live DPS board", "actions": ["refresh", "toggle_pin"]}
  ],
  "settings_schema": {
    "top_n": {"type": "integer", "default": 8, "description": "How many combatants to show in the board."},
    "pin_overlay": {"type": "boolean", "default": false, "description": "Also draw a compact overlay on the DPS meter surface."}
  },
  "permissions": []
}
