{
  "id": "combat_pace_plugin",
  "name": "Combat Pace (Trigger + Timer)",
  "version": "1.0.0",
  "entry": "plugin.py",
  "description": "Custom trigger_type + timer example: registers a 'dps_floor' trigger that fires when sustained DPS drops below a threshold, a 'burst_window' timer, and a UI panel showing the live pace and a manual burst-window countdown.",
  "enabled": true,
  "subscriptions": ["act_snapshot", "encounter_started"],
  "capabilities": [
    "plugin_manager",
    {"id": "trigger_types", "title": "dps_floor trigger"},
    {"id": "timers", "title": "burst_window timer"},
    {"id": "ui_panels", "title": "Pace panel", "actions": ["start_burst"]}
  ],
  "settings_schema": {
    "dps_floor": {"type": "integer", "default": 500000, "description": "Warn when total DPS falls below this."},
    "burst_window_s": {"type": "integer", "default": 15, "description": "Burst window length in seconds."}
  },
  "permissions": []
}
