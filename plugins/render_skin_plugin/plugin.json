{
  "id": "render_skin_plugin",
  "name": "Render Skin / Takeover",
  "version": "1.0.0",
  "entry": "plugin.py",
  "description": "Render-hook example: intercepts the original render of ACT surfaces. In 'skin' mode it rewrites the aggregate/dps payload before drawing; in 'takeover' mode a render hook fully replaces the surface with its own spec (截断原渲染). Disabled by default because it alters built-in UI.",
  "enabled": false,
  "capabilities": [
    "plugin_manager",
    {"id": "render_hooks", "title": "Surface render interception", "actions": ["set_mode"]}
  ],
  "settings_schema": {
    "mode": {"type": "string", "default": "skin", "description": "off | skin | takeover"},
    "surface": {"type": "string", "default": "act_aggregate", "description": "Which surface to intercept (act_aggregate / dps / *)."}
  },
  "permissions": ["render_intercept"]
}
