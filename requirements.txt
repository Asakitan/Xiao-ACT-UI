# SAO Auto — 游戏 HUD 与自动化
# 依赖清单 (完全复刻 28midi SAO-UI, 去除 MIDI 功能)

# ═══ 核心 (与 28midi 一致) ═══
pywebview>=4.0                  # 透明 WebView HUD (EdgeChromium/WebView2)
numpy>=1.20                     # 图像处理 / 鱼眼变换
Pillow>=9.0                     # 截图回退 (PIL.ImageGrab)
mss>=9.0                        # 高性能截图 (优先)
pythonnet>=3.0                  # .NET WinForms 透明 (clr, System.Drawing)

# ═══ 特效 ═══
moderngl>=5.8                   # LinkStart 3D 粒子隧道 (OpenGL)

# ═══ 识图 ═══
opencv-python>=4.5              # 颜色过滤、固定 ROI 条形/技能状态检测

# ═══ 热键 ═══
pynput>=1.7                     # 全局热键监听

# ═══ 音效 ═══
pygame>=2.0                     # SAO 音效系统

# ═══ 抓包数据源 (可选, data_source=packet 时需要) ═══
zstandard>=0.20                 # zstd 解压 (游戏协议压缩)
# dpkt>=1.9                     # 以太网/IP/TCP 解析 (已内置手动解析, 可选)
protobuf>=6.31.1               # SyncContainerData / compiled proto 解码 (全量身份字段必需)
# Npcap 驱动需单独安装: https://npcap.com
